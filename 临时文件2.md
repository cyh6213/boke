# 八、Harness
这个项目里有两个harness概念
第一个是 MindBridgeAgentHarness，它是线上业务链路的一部分。它解决的是 HTTP、Agent、数据库、工具任务混在一起的问题。把一次对话请求包装成一个完整业务单元，输入脱敏、会话解析、Agent run、报告落库、trace 保存、工具计划和消息持久化都在这里统一处理。
第二个是 Engineering Harness，它是工程验收工具。它用临时 SQLite、内存短期记忆和本地输出跑一批可重复用例，验证风险识别、Agent 路由、skills、RAG、API 和工具队列有没有回归。
Harness 的价值不是为了多包一层，而是把“不稳定的模型行为”和“确定性的业务闭环”分开。Runtime 只负责判断和组织模型上下文，Harness 负责业务结果怎么落库、怎么审计、怎么触发后处理。这样系统更容易测试，也更容易解释。
MindBridge 里的 Harness 不是一个单纯的测试脚本，也不是 Agent 本身。它更像项目里两层不同的“运行外壳”，一层包住线上单轮对话，负责把 Agent 的推理结果接到业务闭环里。另一层包住工程验证过程，负责用可重复的环境把风险、安全、路由、RAG、API 和工具队列跑一遍。
1. ##  MindBridgeAgentHarness
它的职责很明确，HTTP 和 SSE 不直接管复杂业务，Agent runtime 也不直接管落库和工具派发。
一次学生输入进来后，Harness 会先保留原始输入，再做隐私脱敏，解析或创建会话，然后调用 Agent runtime。runtime 返回意图、风险评估、检索知识、回复消息和 Agent steps 后，Harness 继续处理用户消息保存、心理报告生成、trace 保存和工具计划。
> 这层设计把聊天接口变薄了，ChatService.stream_chat 只需要调用 agent_harness.run()，把返回的 response_messages 交给模型流式输出，然后保存助手回复，最后再按 tool_plan 派发后台工具，也就是说，接口层关心怎么把内容流给前端，Harness 关心这一轮业务是否完整走完。
MindBridgeAgentHarness.run() 的结果也不是一段文本，而是一个结构化的 AgentHarnessOutcome。这个结果让后续逻辑不需要猜 Agent 做了什么，直接按字段处理
### Agent runtime
Agent runtime 本身负责一轮对话里的 Agent 协作和路由判断。当前代码里有三种 runtime：
默认：`EventDrivenAgentRuntimeService`，由 `AGENT_FRAMEWORK=event_driven_multi_agent` 启用，也是 `app/core/config.py` 的默认值。
任务板 + artifact 协作模式：
```Plain Text
CoordinatorAgent 创建任务板
-> UnderstandingAgent 产出 intent artifact
-> SafetyAgent 产出 risk artifact，并在高风险时发布 SAFETY_OVERRIDE
-> ContextAgent 在 CONSULT/RISK 或中高风险时补充记忆、RAG、skill 上下文
-> ResponseAgent 产出候选回复 prompt
-> SafetyAgent 审查候选回复
-> CoordinatorAgent 最终采纳可用回复方案
```
这套 runtime 的动态路由逻辑是：
- 普通 CHAT：会做意图和安全 artifact，但不会进入心理 RAG，也不会生成心理报告，最后由 ResponseAgent 以普通聊天模式组织回复 prompt。
- CONSULT：会进入 ContextAgent，读取历史、生成记忆摘要、检索心理知识库、选择相关 skill，再由 ResponseAgent 以支持模式组织回复 prompt。
- RISK 或 SafetyAgent 触发 SAFETY_OVERRIDE：风险会提升到 HIGH，回复 prompt 会带高风险安全约束，后续会生成心理报告并进入工具计划。
### Trace
Trace 是这层 Harness 里很值得讲的一点。每轮运行都会保存原始输入、脱敏输入、记忆摘要、路由意图、风险等级、Agent steps、检索证据、回复消息和评估结果。管理员可以用 trace 回看一轮请求为什么被路由成 CONSULT 或 RISK，也能检查某次回复到底用了哪些知识。
2. ##  MindBridgeAgentHarness 的作用
很多 Agent 项目容易把 HTTP、Agent、落库、工具调用混在一起。我们这个项目在外层增加了 `MindBridgeAgentHarness`，它负责单轮 Agent run 的业务编排。简单说，MindBridgeAgentHarness 就是 一次对话请求的总调度员。它本身不是“负责聊天的大模型 Agent”，也不是 HTTP 接口。
流程如下
1. 学生端调用 POST /api/chat/stream，ChatService.stream_chat() 调用 MindBridgeAgentHarness.run()。
2. Harness 对原始输入做 strip()，并生成脱敏后的 model_input。
3. Harness 根据 sessionId 复用会话；没有 sessionId 时新建 ChatSession。
4. Harness 调用真正的 Agent runtime，让 runtime 完成记忆读取、意图路由、知识库检索、风险评估和回复 prompt 规划。
5. Harness 把用户原始消息保存到业务数据库（线上默认 MySQL；harness/测试可切 SQLite），同时把脱敏后的短期记忆写入 Redis。
6. 如果 runtime 输出的 intent 不是 CHAT，也就是 CONSULT 或 RISK，Harness 创建 PsychologicalReport；普通聊天不创建报告。
7. Harness 保存 trace，记录本轮为什么这样路由、用了哪些知识、评估结果是什么、最终准备了哪些回复消息。
8. Harness 生成工具计划。只要有心理报告，就会有工具计划；具体后处理由风险等级决定。
9. Harness 把 AgentHarnessOutcome 返回给 ChatService。
10. ChatService 使用 AiClient.stream(outcome.response_messages) 流式生成最终助手文本，通过 SSE 返回前端。
11. 流式结束后，ChatService 调 Harness 保存助手消息，再派发工具任务。
MindBridgeAgentHarness 的核心价值是让 Agent 输出能被业务系统接住
> - CONSULT/RISK 要生成 PsychologicalReport。
> - CHAT 不能生成心理报告。
> - 有报告才进入工具计划。
> - 高风险报告要创建个案和预警。
> - 普通编程问题不能触发后台预警。
> - 每轮都要保存 trace，方便管理员审计。
它把这些判断集中在一层，避免散落在接口、Agent、数据库服务和工具服务里。你可以这么理解，ChatService 负责“接待和把回复流出去”，AgentRuntime 负责“几个 Agent 怎么思考和协作”，MindBridgeAgentHarness 负责“这一轮业务闭环怎么跑完”。
3. ##   Engineering Harness
它不服务线上请求，而是服务工程验收。启动时它会关闭向量依赖，把邮件预警改成 log 模式，并把 Redis 短期记忆替换成内存实现。这些设置让验证过程不依赖真实 MySQL、Redis、模型服务或邮件服务
当前 Engineering Harness 覆盖 6 类 suite，Risk Safety、Agent Routing、Standard Skills、RAG、API 和 Tool Queue，它测的不是一个小函数，而是完整行为。
> Risk Safety Harness：跑中文高风险、英文高风险、低风险咨询和普通聊天，检查报告是否生成、预警任务是否入队、学生端是否泄露后台字段。
> Agent Routing Harness：强制 agent_framework=custom，检查 CHAT、CONSULT、RISK 三类输入分别走了哪些 Agent。
> Standard Skills Harness：检查标准 skill 是否加载、咨询和高风险场景是否选中正确 skill、交接摘要是否包含关键字段。
> RAG Harness：跑内置 60 条评测 case，计算 Recall@K、Precision@K、MRR、NDCG@K 和 HitRate。
> API Harness：检查健康检查、Basic Auth、学生/管理员权限、聊天 SSE、知识库导入和状态接口。
> Tool Queue Harness：检查高风险报告是否生成 Excel、case、alert 三个任务，也检查依赖、幂等、限流和 dead letter。
我本地 target/harness/rag-eval-report.json 的 RAG 指标如下，大家也可以自测一下
```Plain Text
TopK: 4
Total cases: 60
Recall@K: 0.9667
Precision@K: 0.6458
HitRate: 0.9667
MRR: 0.9083
NDCG@K: 0.9053
```
4. ##  **两者区别**
Agent Runtime Harness 是线上业务链路的一部分。它处理一轮真实用户请求，输出 AgentHarnessOutcome，并把结果写入消息、报告、trace 或工具计划。追求这一轮对话能不能安全、完整、可审计地跑完
Engineering Harness 是工程验证工具。它主动搭环境、重建数据库、替换外部依赖，然后批量跑断言。追求改完代码以后，项目的核心链路有没有回归
最简单的区分方式是
MindBridgeAgentHarness 管单轮业务，app.harness.runner 管工程验收。前者在请求路径里，后者在测试或交付验证路径里。前者产生用户会话、报告和 trace，后者产生 harness-report.json 和 rag-eval-report.json。














# 七、Agent Runtime
Agent Runtime 是整个智能体系统的核心执行内核，专门负责单轮对话内多智能体调度、逻辑推理、上下文组装、安全校验与回复生成，只专注AI思考逻辑，不处理数据库持久化、会话管理、业务报表、日志落库等外部业务操作，所有业务收尾工作交由上层 Harness 处理。

## 1. 现有 Runtime 实现类型
项目内置三套运行时，由环境变量 `AGENT_FRAMEWORK` 切换，默认启用事件驱动多智能体运行时：
1. **EventDrivenAgentRuntimeService（默认）**
   配置标识：`AGENT_FRAMEWORK=event_driven_multi_agent`，在 `app/core/config.py` 设为默认，采用事件分发机制串联多个分工独立的子智能体，流水线式完成一轮对话全推理流程。
2. 简易单智能体 Runtime（基础调试模式）
   仅单一对话Agent，无分层路由、无风险分级逻辑，仅用于本地简单调试。
3. 自定义扩展 Runtime
   供二次开发定制智能体协作流程，配合 Engineering Harness 做路由回归测试。

## 2. EventDrivenAgentRuntime 标准执行流水线
整套运行时依靠 CoordinatorAgent 总控，通过产出标准化 artifact（推理产物）流转数据，完整流程：
1. CoordinatorAgent 创建全局任务板，统一存储本轮所有智能体输出的产物、状态、上下文；
2. UnderstandingAgent 解析用户提问，产出意图 artifact，区分三大对话类型：普通聊天 CHAT、咨询 CONSULT、风险预警 RISK；
3. SafetyAgent 接收意图与原始用户输入，产出风险等级 artifact；识别高风险内容时发布 `SAFETY_OVERRIDE` 全局拦截事件；
4. ContextAgent 按需补充上下文：仅 CONSULT、中高 RISK 场景触发，读取历史会话记忆、摘要、心理知识库RAG检索、加载对应业务技能Skill；纯CHAT场景跳过此步骤；
5. ResponseAgent 整合全部上下文、知识库检索结果、技能信息，生成候选回复提示词；
6. SafetyAgent 二次校验候选回复内容，过滤违规、不当输出；
7. CoordinatorAgent 汇总所有产物，确定最终可用回复方案，封装结构化结果返回上层 Harness。

## 3. 三类对话路由逻辑（Runtime 核心判断能力）
运行时自动根据 UnderstandingAgent 产出的意图自动分流，不同分支执行逻辑完全隔离：
1. **CHAT 普通闲聊**
   仅执行意图识别、基础安全校验；跳过ContextAgent知识库检索、心理报告生成逻辑；ResponseAgent 以轻量化闲聊模板组装回复，不产生后续工具任务。
2. **CONSULT 专业咨询**
   完整走完全部Agent链路；ContextAgent加载心理知识库、历史记忆、专业技能；运行时标记咨询标签，告知上层Harness需要生成心理报告。
3. **RISK 高风险对话 / SAFETY_OVERRIDE 触发**
   提升全局风险等级，回复强制叠加安全约束模板；标记高风险标识，传递给Harness用于生成预警报告、创建个案、推送后台告警工具任务。

## 4. Runtime 能力边界与设计定位
1. 只做内存内推理计算：不操作MySQL、Redis，不保存聊天记录、Trace、报告，不派发后台工具队列；所有数据持久化、业务后置逻辑全部交给外层 MindBridgeAgentHarness；
2. 输出标准化结构化数据：而非纯文本，统一封装推理意图、风险等级、检索证据、Agent分步记录、候选回复等字段，上层Harness无需解析自由文本，直接读取字段做业务处理；
3. 解耦AI逻辑与业务系统：Runtime 仅关心“智能体怎么思考、怎么回答”，Harness 关心“思考完的数据怎么落地、审计、流转业务流程”；更换大模型、调整智能体分工仅需修改Runtime层，完全不改动会话、报表、告警等业务代码。

## 5. Runtime 配套测试验证逻辑
Runtime 全部路由、安全、RAG、技能逻辑均可被 Engineering Harness 自动化覆盖测试，批量输入测试用例后，Harness 校验 Runtime 输出的意图、风险等级、知识库召回结果是否符合预期，快速发现代码修改带来的推理逻辑回归问题。











# 十一、Run Trace 设计
## 1. Run Trace 是什么
MindBridge 里的 Run Trace，可以理解成一轮 Agent 执行的运行轨迹。

## 2. 代码入口
Run Trace 的主链路从聊天接口开始。
学生端调用流式聊天接口后，ChatService.stream_chat() 会先调用 MindBridgeAgentHarness.run()。

Harness 执行核心流程：输入脱敏、会话加载/新建、调用Agent Runtime、采集全链路推理步骤、持久化Trace数据、封装业务输出结果。

实际执行顺序大致为：
1. 接收前端原始用户输入并脱敏
2. 加载或创建会话会话上下文
3. 调用Agent Runtime执行多智能体推理
4. Runtime完成各Agent执行，产出分步执行记录
5. Harness收集所有推理步骤组装Trace数据
6. 持久化AgentRunTrace记录到数据库
7. 生成心理报告（咨询/高风险场景）
8. 生成后台工具执行计划
9. 将结果返回聊天接口流式输出给前端

这个顺序说明，trace 记录的是“生成最终回复前，系统准备给模型的上下文和决策结果”。它不是等模型完整输出以后才补的一段日志。这样做有两个好处。
第一，后台报告和 trace 不会依赖工具任务是否成功。Excel、邮件、个案创建这些后续工具可以异步执行，学生端不会被这些动作卡住。
第二，如果后面模型流式输出或工具派发出错，系统仍然保留了本轮 Agent 的输入、路由、知识召回和风险评估。排查时至少知道系统在回复前走到了哪一步。

## 4. 各链路里的Trace
1. **普通聊天场景**
普通聊天的 trace 很短。比如学生问：“帮我解释一下 Python 列表推导式。”系统通常会走 MemoryAgent -> SupervisorAgent -> CompanionAgent。trace 里会看到：MemoryAgent 读取了 6 条历史消息，SupervisorAgent 判断 intent=CHAT，CompanionAgent 规划普通助手回复。这个 trace 能说明系统没有把普通学习问题强行拉进心理咨询链路，也没有无意义地触发 RAG 和风险评估。

2. **心理咨询场景**
心理咨询场景会走更完整的链路。比如学生说：“我最近考试压力很大，晚上睡不着，白天也很焦虑。”系统会先读记忆，再由 SupervisorAgent 判断为 CONSULT，然后 KnowledgeAgent 改写检索 query，比如“考试压力 失眠 焦虑 校园心理支持”，从知识库召回相关内容。RiskGuardianAgent 会评估风险和情绪，比如 risk=LOW, emotion=ANXIETY。最后 CounselorAgent 组织支持性回复。trace 里能完整记录这五步，无需人工猜测系统判断逻辑。

3. **高风险场景**
高风险场景高度依赖 Trace 用于事后审计追溯。比如学生输入“我不想活了，想结束生命”。这类输入会被路由到 RISK，RiskGuardianAgent 输出风险等级 HIGH。trace 完整记录 KnowledgeAgent 是否检索危机干预知识库、RiskGuardianAgent 风险评估详情、CounselorAgent 高风险应答策略。后续生成心理报告、风险个案、Excel台账、告警记录均可通过 trace 携带的 report_id 关联追溯。

## 5. Trace完整例子
假设学生输入：我最近考试压力很大，晚上睡不着，白天焦虑提不起精神。

系统先脱敏，得到模型输入。该示例无手机号、邮箱、身份证等敏感信息，脱敏前后文本一致。
MemoryAgent 读取历史会话消息，加载本轮对话上下文。
```
step=1
agent=MemoryAgent
action=LOAD_HISTORY
observation=history_msg_count=5
```

SupervisorAgent 判断对话意图：
```
step=2
agent=SupervisorAgent
action=ROUTE_INTENT
observation=intent=CONSULT
```

KnowledgeAgent 改写检索 query，并从知识库召回内容：
```
step=3
agent=KnowledgeAgent
action=RETRIEVE_KNOWLEDGE
observation=query=考试压力 失眠 焦虑 校园心理支持; retrieved=4
```

RiskGuardianAgent 做心理风险与情绪评估：
```
step=4
agent=RiskGuardianAgent
action=ASSESS_RISK
observation=risk=LOW, emotion=ANXIETY
```

CounselorAgent 组装应答上下文，规划回复模板：
```
step=5
agent=CounselorAgent
action=PLAN_RESPONSE
observation=support response planned with risk=LOW
```

全部智能体执行完成后，AgentTraceService.save_run() 将上述所有步骤统一存储为一条 AgentRunTrace 记录。
- 咨询、高风险场景：同步生成心理报告，Trace 中携带 report_id 关联报告；
- 普通闲聊CHAT场景：Trace正常保存，report_id字段为空。