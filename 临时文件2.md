MCP是什么？有哪几个部分？怎么使用？
我理解的 MCP 它就是一个 agent 到 tool 之间的一个协议，它能够让 agent 调用这个tool，避免每一个agent对接每接一个新工具都要单独写适配代码。
底层基于 JSON-RPC 2.0 实现有状态双向通信，支持本地 stdio、HTTP-SSE、流式 HTTP 多种传输方式  
有三部分，Host、Client、Server。Host就是支持MCP协议的应用程序，也就是我们配置大模型了的地方。Client在 Host 内部运行，负责与 MCP Server 建立连接。Server可以理解为一个软件，我们将他提供的原子性的能力称之为Tool，一个Server可以提供多个Tool。
当我们配置并启动我们需要的 Server 后，会获取其包含的 Tool 列表，用户提问后，Host 将问题与 Tool 列表一同传给模型；模型判断需调用的 Tool 及参数，反馈给 Clinet；Clinet 转发调用请求至 MCP Server，Server 执行 Tool 后返回结果；Cline 将结果回传给模型，模型整理后给出最终答案并反馈给用户。
然后包括我的这个 RAG项目，我也把它实现成了一个MCP。我当时想象的场景就是说，我是一个程序员，我肯定不会使用我公司开发的 RAG 来帮我执行一些任务。但是我使用其他，比如说使用 Cloud Code 来执行任务的时候，我又需要获取我公司的资源，怎么办？那我可能就通过公司的 R A G 的 MCP 的一个工具来获取一些资料，然后让 agent 能够更好的帮我完成这个任务。


如何让大模型格式化输出？
● 在原始阶段，就是通过prompt，比如写清楚json skema的例子，但是prompt的约束不稳定
● 指令微调SFT：在 SFT 阶段喂了大量“问题-答案”对，让模型学会（识别什么时候需要调用工具、按固定格式输出函数名 + 参数、不乱说废话，直接吐结构化数据）
● 在输出时的强约束机制：模型是根据概率生成字符，通过微调任然可能输出不符合规范的内容，这时候就可以通过 JSON Schema、正则（ 用来「匹配字符串格式」的专用语法  ）、状态机 过滤，将不符合规范的字符概率设置为0.
具体在SprinAlibaba提供了两种方式
● 直接定义我们需要他返回的类，然后在outputtype里面输入这个类，然后他就会自动将这个类转化为JSON Schema
● 第二种就是我们自己将这个类转化为json，然后放进outputtype


格式化输出 2.0
约束解码：大模型生成文本是一个 token 一个 token 往外蹦的。正常情况下，每一步模型会算出一个概率分布，然后从所有 token 里选一个。但结构化输出做了一件事： 在每一步生成之前，先根据 JSON Schema 算出一个"当前合法的 token 集合"，然后把不合法的 token 的概率全部置为零，模型只能在合法集合里选。
但是好像国产模型不支持，而是通过微调实现的。
我检索了一下说是成本问题：约束解码需要在模型推理的时候，每生成一个 token 都跑一遍状态机，检查当前 token 是否合法。这个状态机本身不贵，但它会破坏推理的批处理（batching）。大模型厂商为了降成本，会把多个用户的请求拼成一个 batch 一起算，这样 GPU 利用率高。但约束解码意味着每个请求的合法 token 集合不一样——你的 Schema 是 {name, age} ，别人的 Schema 是 {city, temperature} ，两个请求的合法 token 集合不同，就没法拼在一起算了。要么只能单独算（降低吞吐），要么用更复杂的调度策略（增加工程成本）。


Skill是啥？（和MCP的关系是啥）
Skill 可以理解为解决特定任务的固定规则，是一种知识的沉淀，来实现策略的复用和传播。
它可以是一个资源文件，一些rules，一个解决什么任务的流程，甚至有的时候比较常用的prompt我也会将其封装为一个skill，在解决特定任务时调用。
整体采用渐进式披露机制，第一层元数据始终加载，包括名称和描述，让模型决定使用哪一个skill；第二层流程层，描述怎么做（流程、规则、约束条件，以及使用哪一个tool，加载哪一个材料）；第三层资源层，是进一步的按需加载，包括资源、示例、脚本、甚至其他的skill等。比如我在些一个skill的时候，为了避免这个skill包含不同任务的内容，我就会在这个skill中写明，当他遇到什么问题的时候，去访问某一个子skill。
MCP 负责提供模型调用外部工具的能力，Skill 给了模型处理一个问题的标准答案，除了提供参考文本之外，还可以提供可直接执行的脚本，这也就是二者功能重叠的部分，Skill 适合轻量脚本，MCP 安全性与稳定性更强，实际场景可结合使用。


HarnessEngineering是什么？
Harness 这个概念本质上是在回答一个问题：为什么同样一个模型，在不同的产品里表现天差地别？答案就是 Agent 等于模型加 Harness，模型提供的是智能引擎，而 Harness 是让这份智能真正落地的那套系统。一个裸模型本质上只是一个文本输入输出的函数，它记不住上下文、执行不了代码、访问不了实时数据。Harness 就是围绕这个函数搭建的一整套基础设施，包括文件系统让它能持久化存储、沙箱让它能安全执行代码、记忆系统让它能跨会话学习、压缩机制防止它在长任务中性能衰减。这些东西的设计原则是从期望的 Agent 行为逆向推导的，你想要 Agent 能做什么，就在 Harness 里给它搭什么能力。所以 Harness Engineering 本质上是一种系统工程思维，它承认模型的智能是不完美的，但通过设计好的环境、工具和反馈循环，可以让任何模型在特定任务上发挥出远超其本身的能力。
Loop Engineering
Loop Engineering到底改变了什么？之前的工作模式是我写 prompt、Agent 干活、我检查结果、再写下一个 prompt，我才是那个驱动循环的人。有了 Loop Engineering 之后，我设计好目标、评估标准和工具集，然后一个循环系统自动去驱动 Agent，我只在最后看结果。但这里有一个关键的前提，我后来才想明白——Loop Engineering 真正适用的场景，是那些停止条件可以被明确验证的任务。比如"让所有测试通过"或者"构建成功"，这些条件 Agent 自己能判断。但对于"做一个好的 PPT"这种任务，什么是好本身就很模糊，你还是需要在循环中介入判断。所以 Loop Engineering 解决的是重复执行的自动化，而不是判断好坏的自动化，这个边界很重要。

Loop Engineering 这个概念，我第一反应是拿它和 Reflection 做对比。Reflection 是 Agent 内部的一次推理过程中自我反思的机制，本质上是模型在生成回答之前的内部循环。而 Loop Engineering 完全不同，它是外部的、跨多次 Agent 调用的编排框架，解决的是让 Agent 能够稳定地长时间自主运行的问题，比如无限循环、任务中断、资源超额消耗这些在实际落地中才会遇到的坑。所以它和 Reflection 不在一个层次上，Reflection 解决的是单次推理的质量，Loop Engineering 解决的是整个任务执行的可靠性。



RAG是什么？RAG解决的问题是什么？
检索增强生成
大模型有三大问题：首先是知识的时效性，在训练好的那一刻就固定了；其次是专业性不足或者说不知道私有知识；最后是幻觉问题，他没有说不知道的能力，只能瞎编。
RAG 就是在调用大模型前，先从知识库里检索相关内容，塞到 Prompt 里，让大模型"看着资料回答。可以让新知识随时入库解决知识时效性的问题；公司文档入库解决私有的问题；让LLM有参考内容，可溯源，解决幻觉的问题。
什么是幻觉？如何避免幻觉？
本质：模型在生成过程中，过度自信地“编造”了信息
原因：
● 大模型生成本质是根据概率输出，所以天然会偏
● Decoder 逐词生成，一旦某一步注意力权重偏了，后续全在这个错误上堆叠。并且向量没有“我不知道”这个状态，遇到知识盲区模型不会沉默，而是用最流畅的语法硬编一个错答案。
类型：
● 训练数据没有包含、检索没有召回到相关内容，导致LLM 没有可用的上下文，靠自身知识在编造答案
● 检索内容召回了但 LLM 没有严格遵循，在文档内容的基础上加了自己的推断。
解决：
● 针对没有找回到相关内容，首先是prompt约束，让他没有的就说不知道；其次是如果混合检索或者Rerank得出来的分数过低，就直接回复没有资料，而不是让大模型对着不相干的内容硬编。
● 针对LLM添加私货的情况，首先是可以把tempreture、top-p等参数调低，让他回答的更严谨，避免跑偏。其次是可以让 LLM 输出结构化的结论，每个结论都必须填写来自哪条参考资料。其次可以是生成后再调用一次LLM核查或者使用RAGAS来做核查，来检查忠实性、答案相关性。